<div class="text-center">
    <h2>{{title}}</h2>
    <p>{{content}}</p>
    <p>{{x}}</p>
    <p>Created by {{user.username}} on {{format_date date_created}}</p>
    <div>
        {{> comments}}
    </div>
    <div>
        {{> comment-form}}
    </div>
</div>
<script data="{{title}}" src="/js/comment.js"></script>